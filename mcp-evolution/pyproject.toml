[project]
name = "mcp-evolution"
version = "0.1.0"
description = "AI 员工进化引擎 MCP 服务"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=3.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "ruff>=0.4",
]
