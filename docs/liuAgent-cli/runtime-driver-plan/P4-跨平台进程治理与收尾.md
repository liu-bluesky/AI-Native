# P4：跨平台进程治理与收尾

- 状态：`pending`
- 前置阶段：P3 的独立提交
- 当前阶段：P4
- 下一计划文档：`none`
- Git 提交：`feat(runtime): complete phase P4 cross-platform runtime hardening`

## 目标

完成 macOS / Windows 的进程树治理、构建验证、完整回归和交付收尾。

## 改造前参考代码

先读取 `docs/liuAgent-cli/runtime-driver-plan/DeepSeek-Harness模块映射.md` 的 P4 节点，再检查其中列出的 DeepSeek 源码和当前项目入口。完成对比后，先更新本阶段方案，再修改代码。

## 实施范围

- 新增或完善 `liuagent_core/process_supervisor.rs`。
- macOS 使用进程组和分阶段终止。
- Windows 使用 Job Object 管理进程树。
- 分离 PowerShell、cmd.exe 和 macOS shell 适配。
- 统一 PathBuf、工作目录、环境变量和输出限制。
- 验证应用关闭、权限不足、大输出和子进程残留场景。
- 完成全量测试、打包检查和文档收尾。

## 验收

- macOS 和 Windows 都能启动、暂停、恢复和取消命令任务。
- 父子进程都能在取消后回收。
- 路径和工作目录行为一致。
- 无界 stdout/stderr 不会拖垮 Runtime 或界面。
- 全量测试和双平台关键用例通过。

## 完成动作

验证通过后按以下顺序执行：

1. 将本文件的 `状态` 改为 `completed`，补充完成日期、实际改动、验证命令和验证结果。
2. 执行：`git diff --check`，确认没有当前阶段之外的修改。
3. 创建提交：`feat(runtime): complete phase P4 cross-platform runtime hardening`。
4. 提交成功后，不再读取新的阶段文档，直接执行全量验证、macOS/Windows 兼容性检查并生成最终交付总结。
