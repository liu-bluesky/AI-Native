我这次只对比了文件和现状，没改代码。

  方案记录.txt:1 写的是一个理想化通用 Agent 编排架构：请求解析、上下文构建、Prompt 栈、规划、澄清、调度、工具执行、上下文合并、继续推理、验证、重试、格式化、记忆分
  类、长期/短期/项目记忆更新。

  当前桌面本地智能体更像一个已落地的 Tauri 本地执行 runtime，入口在 web-admin/frontend/src/views/projects/ProjectChat.vue:24410 调 startNativeLiuAgentLocalChat()，再
  进 web-admin/frontend/src-tauri/src/liuagent_core/mod.rs:1。

  主要区别：

   模块                 方案记录                                            当前桌面智能体
  ━━━━━━━━━━━━━━━━━━━  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   定位                 通用 Agent 工作流蓝图                               项目聊天里的桌面本地模型 + 本机工具 runtime
  ───────────────────  ──────────────────────────────────────────────────  ──────────────────────────────────────────────────────────────────────────────────────────
   Prompt 栈            明确有 Resolve Prompt Stack                         目前主要是 project systemPrompt + desktopAgentGlobalPrompt，在
                                                                            buildLocalLiuAgentSystemPrompt() 拼接
  ───────────────────  ──────────────────────────────────────────────────  ──────────────────────────────────────────────────────────────────────────────────────────
   上下文构建           设计里包括用户输入、历史、长期记忆、文件、图片等    当前传入 historyRows、附件、工作区、项目配置；长期记忆/项目记忆没有形成统一上下文层
  ───────────────────  ──────────────────────────────────────────────────  ──────────────────────────────────────────────────────────────────────────────────────────
   规划/澄清            有显式 Execution Planning、Need Clarification       主要靠系统提示词要求模型“计划先行”，不是独立调度器强制执行
  ───────────────────  ──────────────────────────────────────────────────  ──────────────────────────────────────────────────────────────────────────────────────────
   工具执行             抽象的 Tool Execution                               已实现本地工具：读写文件、搜索、命令、HTTP、MCP 等，并有权限控制
  ───────────────────  ──────────────────────────────────────────────────  ──────────────────────────────────────────────────────────────────────────────────────────
   推理-工具循环        有 Context Merge -> Continue Reasoning              当前确实有模型 step -> tool result -> continuation，但协议兼容还不稳，比如你遇到的
                                                                            reasoning_content 问题
  ───────────────────  ──────────────────────────────────────────────────  ──────────────────────────────────────────────────────────────────────────────────────────
   验证                 有 Fact / Logic / Constraint / Goal 验证引擎        当前没有完整验证引擎，更多是运行结果、工具结果、失败摘要和少量 retry/replan
  ───────────────────  ──────────────────────────────────────────────────  ──────────────────────────────────────────────────────────────────────────────────────────
   Retry Router         明确分流 Search / Plan / Tool / Draft               当前只有有限的失败重试/候选方案记录，不是完整路由器
  ───────────────────  ──────────────────────────────────────────────────  ──────────────────────────────────────────────────────────────────────────────────────────
   Memory Classifier    明确分 Long / Short / Project                       当前主要写 requirement、runtime events、outbox、offline cache，不是真正记忆分类系统
  ───────────────────  ──────────────────────────────────────────────────  ──────────────────────────────────────────────────────────────────────────────────────────
   输出                 Response Formatter                                  当前由模型内容或失败摘要直接形成 assistantMessage
  ───────────────────  ──────────────────────────────────────────────────  ──────────────────────────────────────────────────────────────────────────────────────────
   状态存储             设计里有上下文和记忆存储                            当前有 .ai-employee runtime session、requirement、outbox、offline cache

  结论：方案记录.txt 是完整 Agent 架构目标；当前桌面智能体只是其中“模型调用 + 本地工具执行 + 事件记录 + 离线缓存”的落地子集。它还缺少真正结构化的上下文构建、身份/来
  源判定、验证引擎、记忆分类和稳定的重试路由。
