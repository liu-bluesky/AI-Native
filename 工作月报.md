› '/Volumes/work_mac_1_5T/self/ai-employee/bug.md' 理解需求

• 我先读取 bug.md，只做需求理解和范围梳理，不修改代码。然后我会说明：用户目标、现象、预期结果、可能涉及的模块，以及当前是否存在需
要澄清的地方。

• Explored
└ Read bug.md

─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• 我理解。你描述的不是单纯“缩短提示词”，而是要改变桌面智能体的运行机制：

> 固定提示词只定义真实能力和不可突破的运行边界；收到用户请求后，由 AI 根据具体任务动态形成解决该任务所需的工作上下文，而不是预
> 先把所有场景和行为写死在全局提示词中。

我对 bug.md:1 的需求理解如下。

### 1. 固定提示词不应该预设所有工作流程

现在的问题是，全局提示词可能提前写了大量内容：

- 开发任务应该怎么做
- 问答任务应该怎么做
- UI 任务应该怎么做
- 媒体任务应该怎么做
- 什么时候理解需求
- 什么时候确认
- 什么时候制定计划

这样会造成：

- 每次请求都携带大量无关提示词。
- 简单问答也会收到开发、媒体、部署等规则。
- 固定规则之间容易冲突。
- 模型被固定流程限制，不能根据实际任务选择更合适的方法。

你希望这些内容不要全部预先写死。

### 2. AI 收到请求后，先判断用户行为类型

AI 首先判断用户是在要求：

- 回答问题
- 设计方案
- 分析问题
- 修改代码
- 创建文件
- 执行操作
- 修复故障

这个判断决定后续是“只回答”还是“实际操作”。

例如：

- “给我设计一个登录需求”属于问答/设计任务，不应该修改项目。
- “修改一下登录注册的 bug”属于执行任务，应直接进入代码检查、修改和验证。
- “登录为什么失败”可能是诊断任务，先定位原因；除非用户同时要求修复，否则不自动修改。

### 3. 根据任务动态生成专业工作上下文

你说的“AI 自己生成提示词”，我理解为：

AI 在理解需求后，根据任务所属领域，动态形成一份当前任务专用的工作指令或执行上下文。

例如用户说：

> 修改一下登录注册的 bug。

AI 动态形成的工作上下文大致应该是：

任务类型：代码修复
目标：定位并修复登录注册问题
工作范围：登录页面、注册页面、认证接口、状态管理及相关测试
需要读取：相关源码、项目代码规范、UI 规范
执行方式：定位原因 → 修改代码 → 运行验证
交付内容：问题原因、修改结果、修改文件路径、验证结果

这些内容只服务于当前登录注册任务，不应该永久写进全局 System Prompt。

### 4. 执行型任务应该连续完成

对于明确的修改指令，期望流程是：

理解用户目标
→ 查找相关代码位置
→ 读取与任务有关的代码规范和 UI 规则
→ 分析问题原因
→ 修改代码
→ 运行测试或验证
→ 回复解决了什么问题、修改了哪些文件

如果“修改登录注册 bug”的对象和项目都明确，就不应该再机械询问：

> 是否确认开始修改？

只有以下情况才需要停下来：

- 无法判断要修改哪个对象。
- 存在两种结果明显不同的合理理解。
- 涉及删除、覆盖、部署等高风险操作。
- 缺少必须由用户提供的业务决定。

### 5. 问答型任务只生成答案，不进入修改流程

例如：

> 给我设计一个登录需求。

AI 应该动态切换到产品设计或需求分析领域，形成类似这样的内部工作上下文：

任务类型：需求设计
专业领域：产品设计、身份认证、账户安全
输出目标：形成可实施的登录需求
需要覆盖：使用场景、功能流程、异常状态、安全要求、验收标准
禁止行为：不修改项目文件，不执行部署

然后直接输出登录需求方案。

不是先向用户介绍“我有哪些工具、我会哪些能力”，也不是加载代码修改和部署规则。

### 6. “AI 自己生成提示词”需要有边界

这里不能让 AI 生成新的高权限 System Prompt，也不能通过动态提示词创造不存在的功能。

合理的分层应该是：

固定 Runtime 契约
↓
判断用户意图
↓
生成当前任务的工作上下文
↓
读取相关规范、文件和工具信息
↓
回答或执行
↓
验证并交付结果

其中：

- 固定层只保留真实工具、权限、安全和数据边界。
- 动态层负责当前任务的专业角色、目标、范围、步骤和交付标准。
- 动态层不能绕过 Runtime 权限。
- 动态层不能声明不存在的工具。
- 与当前任务无关的专业规则不进入上下文。

所以，你的核心要求可以概括成一句话：

> 把桌面智能体从“依靠一个庞大全局提示词预设所有行为”，改成“最小固定运行契约 + AI 按当前需求动态生成任务上下文 + 根据意图决定回
> 答或直接执行”。

我认为这个需求描述是清楚的。这里的“生成提示词”本质上是生成当前任务的专业工作上下文，而不是让模型随意改写最高层系统规则。
