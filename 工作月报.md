• 已经定位并修复。这次不是 TaskProfile 提示词字段写错，也不是 1.0 的问题，而是流式响应被重复拼接了。

实际请求记录显示，2026 年 7 月 30 日 17:37:35，gpt-5.6-sol 在“你好”请求的 Task Router 阶段返回了一个 JSON，但 Runtime 在第 307
列后又拼接了另一段内容。

原来的处理类似：

content.push_str(&value);

它假定 provider 每次返回的都是增量：

第1段：{"version":
第2段："task-profile/v1",
第3段："intent":"answer"}

拼起来是正常的：

{"version":"task-profile/v1","intent":"answer"}

但该 OpenAI-compatible provider 可能返回累计快照：

第1段：{"version":"task-profile/v1"
第2段：{"version":"task-profile/v1","intent":"answer"
第3段：{"version":"task-profile/v1","intent":"answer"}

旧 Runtime 直接追加后就变成：

{"version":"task-profile/v1"
{"version":"task-profile/v1","intent":"answer"
{"version":"task-profile/v1","intent":"answer"}

或者：

{"完整的TaskProfile"}{"完整的TaskProfile"}

第一个 JSON 已经解析完成，后面又遇到字符，因此 serde_json 报：

所以不是新增提示词把 JSON 写坏了，而是：

新增强制 Task Router

### 已完成的修复

我没有放宽 TaskProfile Schema，也没有截取第二个 JSON 作为兜底，而是在 OpenAI-compatible 流式协议层修复内容合并：

- 普通增量片段：继续顺序追加。
- 累计快照：用最新完整快照替换旧快照。
- 完全重复内容：忽略重复。
- 前后有重叠的片段：只追加不重复的部分。
- content 和 reasoning_content 使用同一套规则。

修改位置：

- web-admin/frontend/src-tauri/src/liuagent_core/runtime.rs:10594
- web-admin/frontend/src-tauri/src/liuagent_core/runtime.rs:15141
- web-admin/frontend/src-tauri/src/liuagent_core/runtime.rs:15168

验证结果：

- 累计 JSON 快照测试通过。
- 正常流式增量测试通过。
- 原 Task Router 测试通过。
- clarityScore: 1.0 修复继续通过。
- clarityScore: 1.5 仍然严格拒绝。
- 296 项非网络 Rust 测试通过。
- 前端本地聊天检查通过。
- 格式、编译和差异检查通过。
- 没有增加旧提示词、兼容模式、分类回退或静默降级。
- 没有修改数据库，没有部署。

需要注意：当前修改在源代码中。如果桌面应用仍运行的是修改前的构建，需要重新编译并重启桌面应用后再测试；仅刷新聊天页面不一定会加
载新的 Rust Runtime。
