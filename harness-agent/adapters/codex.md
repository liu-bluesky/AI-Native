# Codex 适配说明

在 Codex 中使用时：

1. 遵循项目根目录 `AGENTS.md` 的宿主规则。
2. 将 `harness-agent/AGENT.md` 作为本项目开发 Harness 角色说明。
3. 执行具体任务前按需读取对应技能文件，不要一次性加载全部技能。
4. 文件修改使用项目既有编辑方式，验证使用项目真实脚本。
5. 发现宿主规则与本适配说明冲突时，以宿主规则为准。
