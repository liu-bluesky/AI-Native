[project]
name = "mcp-persona"
version = "0.1.0"
description = "AI 员工人设管理 MCP 服务"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=3.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "ruff>=0.4",
]
