Hermes 的核心做法是分层：

1. 工具层只保证契约和写入安全
   write_file(path, content) 仍然要求 content 是字符串，不接受对象。工具层负责原子写入、stale file 检查、并发写
   保护、BOM/换行保留等，不负责判断 package.json 业务上写得好不好。

2. 写完后走通用诊断层
   Hermes 在 write_file/patch 后接 lint 和 LSP diagnostics。代码里明确说，TS/Go/Rust 的单文件命令会产生假阳性，
   所以用 LSP 替代：tsserver、gopls、rust-analyzer。见：
   /Volumes/work_mac_1_5T/hermes/hermes-agent/tools/file_operations.py:397
   /Volumes/work_mac_1_5T/hermes/hermes-agent/tools/file_operations.py:1236

3. 语言支持靠 LSP registry，不靠每种语言写一个工具
   Hermes 有统一的 LSP server registry：pyright、typescript、vue-language-server、gopls、rust-analyzer、clangd
   等。新增语言本质是新增一个诊断 provider，不是新增一套 agent 工具。见：
   /Volumes/work_mac_1_5T/hermes/hermes-agent/agent/lsp/servers.py:831

4. 扩展靠 hooks，而不是改核心
   Hermes 支持 pre_tool_call、post_tool_call、pre_llm_call 等 hook。比如 post_tool_call 可以匹配 write_file|
   patch 后执行 formatter、policy checker、项目自定义校验。见：
   /Volumes/work_mac_1_5T/hermes/hermes-agent/cli-config.yaml.example:1091

所以正确设计不是“加 package.json validator”，而是：

你的 agent 要加的是 Verification Gate / Acceptance Gate。

它应该这样工作：

1. 项目发现
   自动识别当前项目有什么：package.json、Cargo.toml、go.mod、pyproject.toml、pom.xml、Makefile、CI 配置、已有脚
   本。

2. 生成验证计划
   不是让模型随便写“验证方案”，而是 runtime 根据项目事实生成：
   - 改了 JSON/YAML/TOML：先做语法解析。
   - 改了 TS/Vue：跑 LSP/typecheck/build。
   - 改了 Rust：跑 cargo check/test。
   - 改了 Go：跑 go test ./... 或 gopls。
   - 有项目脚本：优先使用项目已有 lint/typecheck/test/build。
   - 没有验证能力：明确标记 verification gap，不能说完成。

3. 执行后收集证据
   每次工具写入后记录 changed files，然后 gate 决定要跑哪些检查。检查结果回灌模型，不是只靠模型自述。

4. 语义质量用项目规范和 reviewer，不塞进工具
   你举的 package.json 里 "latest"、依赖分组不合理，这种很多时候 build 也能过。它应该由两类东西拦：
   - 项目/组织 policy hook，例如“禁止 latest 依赖”。
   - 独立 reviewer pass：拿用户目标、diff、诊断结果、项目现有风格，输出结构化 verdict。

5. 大型迁移要分批闭环
   对大型迁移，不能一次性“全项目改完再验证”。应该是：
   - 发现项目结构和验证能力。
   - 建 migration plan。
   - 分批改动。
   - 每批跑局部验证。
   - 最后跑全量验证。
   - 任何验证缺口都显式报告。

结论：你之前的问题不是“AI 不聪明”，也不是“提示词不够专业”。根因是 agent runtime 缺少通用的验证闭环。Hermes 的方
向是：工具严格、诊断通用、扩展走 hook、语义质量走项目规范和 reviewer。你这个 agent 也应该按这个架构修，而不是继
续给前端、后端、配置文件分别写硬编码 validator。
-+------------------------
。 1. 项目聊天
web-admin/frontend/src/utils/ws-chat.js:156 仍有 /api/projects/{project_id}/chat/ws，后端对应 web-admin/api/routers/projects.py:23811。
虽然 web-admin/frontend/src/views/projects/ProjectChat.vue:27512 已经有本地 liuAgent 路径，但项目聊天还没完全脱离后端 runtime。

2. 项目聊天里的普通全局后端调用
   web-admin/frontend/src/views/projects/ProjectChat.vue:28243 还会 POST /projects/chat/global，后端是 web-admin/api/routers/projects.py:15176。

3. 项目工作日志 AI 汇总
   web-admin/frontend/src/views/desktop/ProjectWorkLog.vue:1017 也走 /projects/chat/global，迁全局助手 UI 不会自动改掉它。

4. 全局助手任务 / 定时提醒 / 事件触发任务
   前端 web-admin/frontend/src/utils/task-store.js:220 仍调用 /projects/chat/global/tasks。后端执行在 web-admin/api/services/assistant/
   global_assistant_task_service.py:1293，调度器在同文件 web-admin/api/services/assistant/global_assistant_task_service.py:1368。

5. 飞书机器人 / 飞书提醒
   web-admin/api/services/feishu/feishu_bot_service.py:65 和 web-admin/api/services/feishu/feishu_scheduled_reminder_service.py:13 都依赖
   global_assistant_task_service。

6. 全局助手语音 ASR/TTS
   web-admin/frontend/src/components/GlobalAiAssistant.vue:2863 仍调用语音 runtime、转写、TTS、欢迎语音这些后端接口；后端路由在 web-admin/api/
   routers/projects.py:13592 附近。只迁文本对话，不会迁语音。

7. 后端 runtime 工具桥
   web-admin/api/services/tool_executor.py:461 仍有 global_assistant_builtin_tool 分支；agent_runtime/v2 里也还有 browser bridge 相关处理。只要项目
   聊天、任务、飞书事件还走后端 runtime，这些就不能删。

另外，全局悬浮助手现在在桌面端被直接隐藏：web-admin/frontend/src/components/GlobalAiAssistant.vue:1225。要改成本地 Tauri 常驻，第一步就是反过来让它
在 Tauri 渲染，并把 web-admin/frontend/src/utils/ws-chat.js:181 的全局助手 WS 执行链换成 web-admin/frontend/src/utils/native-desktop-bridge.js:311
的 startNativeLiuAgentLocalChat。

MCP 记录：项目 proj-d16591a6，本轮查询任务树 tts-5e8af20014 已归档完成。

---

2. 还有一些关键词提取 metadata
   - web-admin/frontend/src-tauri/src/liuagent_core/runtime.rs:3132 infer_requirement_capability
   - web-admin/frontend/src-tauri/src/liuagent_core/runtime.rs:3144 infer_requirement_constraints
   - web-admin/frontend/src-tauri/src/liuagent_core/runtime.rs:3169 build_context_lookup
   - 这些主要写 requirement 摘要，不应该决定搜索或任务树。

3. 任务树里还保留 intent 分支
   - web-admin/frontend/src-tauri/src/liuagent_core/planning.rs:163
   - match task_goal.intent.as_str() 还支持 question/analysis/documentation/bugfix/governance/...
   - 但现在 build_task_goal 固定传 agentic_request，所以正常路径会走默认三步，不会再命中文档分支。
   - 如果别的地方手动构造 TaskGoal.intent = documentation，它仍会走旧分支。

4. 工具级规则映射
   - tool_candidate_score
   - tool_risk_level
   - tool_verifiability
   - tool_verification_plan
   - permission_action_for_tool
   - 这些是按工具名做风险、排序、权限、验证计划映射。Hermes 也有类似 registry/toolset/check_fn 思路，只是它集中在 registry，而不是散在 runtime 里。

5. 最终回答 URL 校验规则
   - evaluate_external_url_evidence
   - 这个是刚加的：最终回答有 URL，必须有 http_get 2xx 证据；明确写“未核验/无法核验”才允许作为未验证说明。

如果要继续向 Hermes 靠，下一步最该动的是：把 classify_clarity_task_type 和 planning.rs 的 intent 分支也弱化/删除，保留安全确认用的显式风险判断，不再用中文关键词做任务类型路由。

---

• 对比完了，结论是：Hermes 的方向更合理，当前系统应该按这个方向收敛。我已经把当前系统里残留的“web_search 后必须 http_get 核验”的提示去掉了，没有做飞书路径或模板 URL 的特判。

Hermes 做法：

- /Volumes/work_mac_1_5T/self/hermes-agent/tools/web_tools.py:1034：web_search_tool 只返回搜索元数据：title/url/description/position。
- /Volumes/work_mac_1_5T/self/hermes-agent/tools/web_tools.py:2082：web_search 和 web_extract 是两个独立工具，搜索不强制等于打开网页。
- /Volumes/work_mac_1_5T/self/hermes-agent/model_tools.py:307：工具不可用时从 schema 里剔除相关引用，避免模型幻觉调用不存在的 web 工具。
- 没看到 Hermes 在最终回答阶段按 URL 字符串做“必须被某个 http_get 成功核验”的阻断。

当前系统原问题：

- web-admin/frontend/src-tauri/src/liuagent_core/definitions.rs:177 之前还在告诉模型“引用事实前必须 http_get”。
- web-admin/frontend/src-tauri/src/liuagent_core/runtime.rs:5248 的工具验证计划也残留了同样约束。
- 这会诱导 agent loop 走错误逻辑：搜索结果里只要有模板接口 URL、文档 URL、endpoint 示例，就可能被运行时当成“未核验 URL”处理，和你说的一样，本质不应该靠条件判断校验。

我改后的当前系统：

- web-admin/frontend/src-tauri/src/liuagent_core/definitions.rs:177：web_search 现在描述为候选信息，由模型根据目标、结果质量、任务风险判断是否继续打开页面。
- web-admin/frontend/src-tauri/src/liuagent_core/runtime.rs:5248：验证计划改成“需要更完整正文或更高置信度时，可继续打开页面或补充核查”。
- web-admin/frontend/src-tauri/src/liuagent_core/runtime.rs:5368：外部 URL gate 现在是 informational only，passed: true，不会因为 URL 没被 http_get 字符串匹配而阻断最终回答。

还存在的能力差距：

- 当前系统 web_search 后端只有 Managed URL / Tavily，Hermes 有 Firecrawl / Parallel / Tavily / Exa。
- 当前 Tauri 侧没有 Hermes 那种独立 web_extract 工具；深度正文读取主要还是靠 http_get。
- 这不是兼容问题，而是能力差距。搜索回答不应被阻断，但需要正文抽取时，后续可以补一个通用 web_extract 能力。

验证已跑：

- cargo fmt
- cargo test external_url
- cargo test tool_definitions_include_web_search_when_backend_configured
- cargo check

cargo check 通过，只剩已有 unused 警告。MCP 资源目录本轮没有暴露 query://usage-guide 等资源，所以远端任务树闭环未完成；本地代码对比和修改已完成。

---

• 理解。你要的不是“JSON 配置编辑器”，而是类似截图这种设置中心布局：

- 左侧是设置导航。
- 右侧是“技能与命令”主面板。
- 每个能力是一行模块，不是大文本框。
- 行内包含：
  - 图标
  - 名称
  - 简短说明
  - 启用/停用开关
  - 设置按钮

- 顶部有搜索。
- 有“全局 / 项目”切换。
- JSON 只能是高级入口，不能作为主要交互。
- 操作要像设置项一样直接、可扫描、可管理。

我刚才已经把 web-tools UI 往这个方向改了一版：Provider 列表、项目/全局切换、开关、设置弹窗、高级 JSON 折叠。

但严格按你这张图看，还差几个点：

- 当前是在 MCP 与护栏 里加了一块，不是独立的“技能与命令”式设置页。
- 搜索框还没加。
- Provider 行的视觉密度、图标、分页/分组还不够像截图。
- “技能”和“命令”两个分区的结构还没有统一抽象。
- 全局/项目 tab 应该靠近列表标题，而不是只在卡片头部。

所以正确方向应该是：把 Web Tools / Skill / Plugin / MCP 这种可配置能力统一成一个“能力模块列表”设置交互，而不是每种能力单独塞 JSON 面板。

---

• 已改完。核心变化是飞书机器人和飞书提醒不再默认带 admin 权限跑，而是按机器人/任务 owner 的登录用户构造权限与运行环境：

- web-admin/api/core/deps.py:73：新增 build_user_auth_payload()，从 user_store 解析真实 role/roles。
- web-admin/api/services/feishu/feishu_bot_service.py:3605：飞书消息进入项目对话时传真实用户 auth_payload，开启
  auto_use_tools=True，不再返回“桌面端接管队列尚未接通”。

- web-admin/api/services/assistant/global_assistant_task_service.py:1114：全局助手动态 project_chat / system_speech 改用任务创
  建人的权限。

- web-admin/api/services/feishu/feishu_scheduled_reminder_service.py:281：飞书定时提醒发送时用任务 owner 构造 CLI plugin
  runtime env。

- web-admin/api/services/feishu/feishu_bot_service.py:1942：顺手修了归档提示词优先使用 agent_name。

现在机器人权限逻辑：它能做的事取决于绑定/执行用户在系统里的角色权限和项目成员权限，例如项目列表、项目切换、项目对话、工具调用等
都走该用户权限；不再因为来自飞书就自动是 admin。当前后端还没有独立“桌面端接管队列”，所以实际落点是“项目对话运行时 + 登录用户
auth_payload + owner-scoped plugin/runtime 环境”。

当前机器人已有功能主要包括：飞书连接器凭据检查、列出机器人所在群、列群成员、按群名解析群、接收群聊/私聊消息、下载图片/附件、回
复飞书消息、绑定或复用项目聊天会话、调用项目对话、处理自动归档/结构化记录、匹配全局助手任务、创建会议提醒、发送定时飞书提醒、系
统播报通知。

验证已通过：

• 可以做到“接近项目对话框”，但现在还不是 100% 等同。

现在飞书消息已经会走项目对话运行时，并带上登录用户权限，所以这些能力原则上可以通过飞书触发：

- 项目列表、项目查询：可以，前提是项目对话工具本身对该用户开放。
- 切换项目：可以做成命令或自然语言绑定，但飞书没有前端“当前选中项目”状态，必须靠群/私聊会话绑定、显式项目名，或新增“切换项目”命
  令来更新绑定。

- 编写/修改代码：取决于项目对话运行时是否能访问对应 workspace 和工具权限；权限会按登录用户走。
- 打包/构建：只有在项目对话/桌面 Runner 可执行本地命令、且用户确认命令后才应该执行。不能让飞书机器人绕过确认直接跑打包。
- 部署/上线：必须走项目部署配置和单独确认，不能只靠飞书一句话直接生产部署。

关键限制是：当前后端还没有一个真正独立的“桌面智能体接管队列”。我这次改的是让飞书进入项目对话运行时，并使用登录用户的
auth_payload 和 owner-scoped runtime env。也就是说，权限已经跟用户走了，但“完全等同桌面项目对话框里的本地 Runner 执行体验”还需
要补一层飞书会话到桌面 Runner/项目选择状态的桥接。

如果你要达到你说的效果，下一步应该补两个点：

1. 飞书会话项目绑定/切换命令：例如“切换到 CRM 项目”“当前项目是哪个”“列出我有权限的项目”。
2. 飞书触发本地执行桥接：编写、测试、打包时复用项目对话的 Runner 能力，并对命令、目录、产物路径、影响范围做确认。

---

• 已处理。这个报错不是“大模型配置不能用”，根因是飞书消息还在走旧的后端 Python 长连接 worker，进入了
project_chat_external_agent_task_store.enqueue，而当前 CORE_STORE_BACKEND=postgres 不支持这个旧队列，所以机器人只回了“收到，正在处
理”后就断在后台。

我已经把这条旧后端路径关掉了：

- 后端不再启动/重启 feishu_long_connection_worker.py
- 删除/禁用 FeishuBotLongConnectionSupervisor
- process_feishu_message_event() 现在只是保护性返回，不再入队
- start_api_with_runner.sh 不再启动飞书 worker
- stop_api_with_runner.sh 只保留清理历史残留旧进程
- 机器人消息监听和执行边界收敛到桌面 Tauri sidecar

现在结论是：飞书连接应跟着桌面智能体一起启动，不需要每次手动重启后端 worker。当前不是通过 lark-cli event consume 做长期连接，也不是后
端 Python worker；机器人运行链路应是桌面 Tauri 侧边车使用飞书 SDK 监听，然后走桌面智能体同一套本地能力和工具。

后端还会保留机器人配置、诊断、辅助接口，但机器人消息业务逻辑不再放在后端服务里。提示词也按你的要求：只用机器人配置里填写的提示词，空就
不注入内置提示词。

验证已过：

- 后端相关 py_compile 通过
- start_api_with_runner.sh / stop_api_with_runner.sh 语法检查通过
- 后端 worker disabled / postgres no-enqueue 回归测试通过
- npm run test:local-bot-runtime 通过
- npm run tauri:check 通过
- 当前机器已确认没有残留 feishu_long_connection_worker.py 进程

你这边要生效，需要重启一次后端和桌面应用；之后保持桌面智能体运行即可。后续如果飞书没回复，应该看桌面 Tauri sidecar 日志，而不是再看后
端 feishu_bot_service 这个旧 worker 报错。
