我按“调用前不强制传入 model_name，而是允许空值后回退到 provider 默认模型或第一个模型”来判定。检索到这些 AI 调用位置没有明确要求模型：

1. 项目聊天 / 项目执行对话
   web-admin/api/models/requests.py:562 的 ProjectChatReq.provider_id/model_name 默认空；web-admin/api/routers/projects.py:4543 通过默认 provider/model 兜底。
2. 全局助手相关调用
   web-admin/api/routers/projects.py:4510 允许请求不传模型，使用系统配置或默认 provider/model；例如全局任务分类调用在 web-admin/api/routers/projects.py:10884。
3. 部署命令 AI 生成
   web-admin/api/models/requests.py:328 的 provider_id/model_name 默认空；生成逻辑走全局助手 runtime，见 web-admin/api/routers/projects.py:1435。
4. 员工草稿生成
   web-admin/api/models/requests.py:175 的模型可空；实现会选默认 provider/default_model，最后还硬兜底 "gpt-4"，见 web-admin/api/routers/employees.py:1639。
5. 智能体模板翻译 / 去重
   web-admin/api/models/requests.py:211 的模型可空；web-admin/api/routers/agent_templates.py:748 会取默认模型。
6. 项目 Studio 文本类生成：提取 / 分镜
   web-admin/api/models/requests.py:493 和 web-admin/api/models/requests.py:504 模型可空；web-admin/api/routers/projects.py:1832 会取默认模型。
7. 反馈反思 / 反馈升级分析
   web-admin/api/models/requests.py:1000 里 model_name 默认空；反思目标解析会回退到配置或默认 provider/model，见 web-admin/api/services/feedback_service.py:461。
8. 项目经验总结 / 经验规则融合
   web-admin/api/models/requests.py:1057 的 model_name 可空；实现会用 provider 默认模型或第一个模型，见 web-admin/api/routers/projects.py:17267。融合入口也允许 provider/model 空并从项目聊天配置兜
   底，见 web-admin/api/routers/projects.py:17344。
9. LLM Provider 连通性测试
   web-admin/api/models/requests.py:1176 的 model_name 默认空；测试逻辑会取 provider 默认模型，见 web-admin/api/services/providers/llm_provider_service.py:2277。

底层共同兜底点在 web-admin/api/services/providers/llm_provider_service.py:701 和 web-admin/api/services/runtime/provider_resolver.py:71：空 model_name 会被替换成 default_model 或模型列表第一项。

明确要求模型的地方也有：例如 Studio 角色参考图、Studio 语音生成/克隆会直接校验 model_name is required，见 web-admin/api/routers/projects.py:13898 和 web-admin/api/routers/projects.py:14098。

只做了只读检索，没有改业务代码或跑测试；本轮仅新增/更新了 .ai-employee 下的查询记录。

• 已修复。根因不是 MCP 工具“不知道部署”，而是客户端实际读取的 query://usage-guide / query://client-profile/codex 之前没有部署产物技能约束，所以 AI 只知道通用 MCP 流程，不知道该走 push_project_depl
oy_artifact，于是开始乱扫本地历史发布配置。

我做了这些改动：

- 新增部署技能：mcp-skills/knowledge/skill-packages/project-deploy-artifact/
- 把部署约束写入：
  - query://usage-guide
  - query://client-profile/codex
  - query-mcp-workflow
  - 系统 bootstrap prompt 模板
- 明确部署流程：
  - 客户端 AI 只打包、计算 artifact 元数据
  - 调用 push_project_deploy_artifact 推送到服务端
  - 服务端按项目 deploy_settings 创建 ProjectDeployArtifact / ProjectDeployRun 并部署
  - 自动部署关闭时，产物列表显示“部署”按钮，或明确授权后调用 deploy_project_deploy_artifact
- 禁止客户端部署时扫描历史发布配置、CI 配置、本地凭据、远端脚本、环境变量
- 修了 query MCP 里 hashlib / datetime 缺失导入导致的工具报错
- 修了 get_query_mcp_cli_prompt_preview 返回结构不稳定的问题

验证结果：

- 84 passed, 259 deselected
- py_compile 通过
- git diff --check 通过
- 直接检查 build_query_usage_guide_text() 和 build_query_client_profile_text("codex")，确认都包含：
  - project-deploy-artifact
  - push_project_deploy_artifact
  - deploy_project_deploy_artifact
  - 禁止扫描历史发布配置的约束

注意：最后尝试用当前 MCP 服务写回工作事实时，服务端仍报 name 'datetime' is not defined。本地代码已经修了这个问题，但运行中的 MCP/API 服务还没加载新代码，需要重启或刷新 web-admin/api 服务后才会生
效。
