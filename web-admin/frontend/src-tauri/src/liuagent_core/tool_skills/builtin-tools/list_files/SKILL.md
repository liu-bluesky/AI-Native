# list_files

## 作用

列出当前 workspace 内的目录内容，用于确认目录结构和发现目标文件。

## 选择时机

- 不知道文件位于哪里。
- 需要先了解项目结构再读取或修改。

## 参数

`path` 默认 `.`；`max_depth` 控制深度；`include_hidden` 控制是否包含隐藏文件。

## 边界

只读取 workspace 范围，不要用它递归扫描无关的系统目录。

