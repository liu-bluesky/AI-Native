# web_search

## 作用

搜索网络信息，返回标题、URL、摘要和来源。

## 选择时机

- 用户需要外部最新信息、资料或候选来源。
- 不知道目标网页地址，需要先发现资料。

## 参数

必须提供 `query`；可选 `limit` 和 `timeout_ms`。

## 边界

结果只是候选信息；高风险或时效性结论应继续使用 `web_extract` 或可靠来源核查。未配置搜索后端时该工具不可用。

