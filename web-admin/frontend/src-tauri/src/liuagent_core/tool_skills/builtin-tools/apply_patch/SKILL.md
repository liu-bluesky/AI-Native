# apply_patch

## 作用

使用 unified diff 对 workspace 内已有文件进行局部修改。

## 选择时机

- 已经明确修改位置和上下文。
- 需要保持最小 diff、避免覆盖用户已有改动。

## 参数与权限

必须提供完整 `patch` 和 `summary`；这是中风险写操作，执行前遵守用户授权。

## 边界

先读取目标文件；不要用整文件重写覆盖无关改动。

