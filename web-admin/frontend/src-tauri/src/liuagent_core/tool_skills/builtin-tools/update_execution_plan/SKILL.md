# update_execution_plan

## 作用

为确实需要多个步骤的任务创建或更新执行计划。

## 选择时机

- 修改代码、配置、测试并验证的多阶段任务。
- 需要持续跟踪进度或从中断点恢复的任务。

## 不要使用

- 简单问答、单文件小改动或一次性查询。

## 参数与规则

`steps` 按执行顺序排列，数量 2-8；每项包含 `title` 和 `status`。同一时刻最多一个 `in_progress`，已完成步骤不能退回。

